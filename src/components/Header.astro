---
import { Icon } from "astro-icon/components";
import { SITE } from "../consts";

const links = [
  { href: "#about", label: "About" },
  { href: "#skills", label: "Capabilities" },
  { href: "#services", label: "Services" },
  { href: "#projects", label: "Works" },
  { href: "#faq", label: "FAQ" },
];
---

<header class="sticky top-0 z-40">
  <div
    class="absolute inset-0 -z-10 border-b border-charcoal-ink/12 bg-cream-white/85 backdrop-blur-xl"
  >
  </div>

  <div
    class="mx-auto flex max-w-7xl items-center justify-between gap-6 px-6 py-4"
  >
    <a href="/" class="group flex items-center gap-3 text-charcoal-ink">
      <span
        class="relative inline-flex size-9 items-center justify-center bg-charcoal-ink text-pale-sage type-caption-12 tag-num"
      >
        {SITE.name.charAt(0)}
      </span>
      <div class="flex flex-col leading-tight">
        <span class="type-link-16 text-charcoal-ink">{SITE.name}</span>
        <span class="type-caption-12 uppercase tracking-[0.2em] text-forest-teal"
          >Studio · MMXXVI</span
        >
      </div>
    </a>

    <nav class="hidden md:block">
      <ul class="flex items-center gap-1">
        {
          links.map((l, i) => (
            <li class="flex items-center gap-1">
              {i > 0 && <span class="text-charcoal-ink/25">·</span>}
              <a
                href={l.href}
                class="type-caption-12 uppercase tracking-[0.18em] px-3 py-2 text-charcoal-ink/70 transition hover:text-charcoal-ink"
              >
                {l.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <a
      href="#contact"
      class="hidden md:inline-flex items-center gap-2 border border-charcoal-ink bg-charcoal-ink px-5 py-2.5 text-pale-sage transition hover:bg-forest-teal hover:border-forest-teal"
    >
      <span class="type-caption-12 uppercase tracking-[0.18em]">Let's Talk</span
      >
      <Icon name="tabler:arrow-up-right" class="size-3.5" />
    </a>

    <button
      id="mobile-menu-toggle"
      class="md:hidden p-2 text-charcoal-ink"
      aria-label="Toggle menu"
    >
      <Icon name="tabler:menu-2" class="size-6" />
    </button>
  </div>

  <nav
    id="mobile-menu"
    class="hidden md:hidden border-t border-charcoal-ink/12 bg-cream-white/95 backdrop-blur-xl"
  >
    <ul class="flex flex-col gap-1 px-6 py-4">
      {
        links.map((l) => (
          <li>
            <a
              href={l.href}
              class="type-link-16 block px-3 py-3 text-charcoal-ink transition hover:bg-pale-sage"
            >
              {l.label}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header>

<script>
  const toggle = document.getElementById("mobile-menu-toggle");
  const menu = document.getElementById("mobile-menu");
  toggle?.addEventListener("click", () => menu?.classList.toggle("hidden"));
  menu
    ?.querySelectorAll("a")
    .forEach((a) =>
      a.addEventListener("click", () => menu?.classList.add("hidden")),
    );
</script>
